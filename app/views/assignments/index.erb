<h2>Assignments</h2>
<a href="/courses/<%= course.id %>/assignments/new">+ Create A New Assignment</a>
<ul>
	<% course.assignments.each do |assignment| %>
	<li>
		<a href="/assignments/<%= assignment.id %>"><%= assignment.name %></a>
	</li>
	<% end %>
</ul>